services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    user: 1000:1000 
# Uncomment for Intel/AMD users
#    group_add:
#      - "122"        # Replace with your host's 'render' group ID
#    devices:
#      - /dev/dri:/dev/dri  # Maps the host GPU to the container
#
# Uncomment for NVIDIA users     # Ensure 'nvidia-smi' works on your host beforehand
#    environment:
#      - NVIDIA_VISIBLE_DEVICES=all
#      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              count: 1
#              capabilities: [gpu]
    network_mode: host
    volumes:
      - ./config:/config
      - ./cache:/cache
      - /data/media:/media
    restart: unless-stopped
